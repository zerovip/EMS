<!DOCTYPE html>
{% load staticfiles %}
<html>
        <head>
                <title>首页</title>
        </head>
        <body>
        {% if status == 0 %}
        <form action="/emsys/login/" method="post">
                {% csrf_token %}
                <p>请登录：</p>
                <p>用户名：<input type="text" name="username" /></p>
                <p>密码：<input type="password" name="password" /></p>
                <input type="submit" value="登陆" />
                <p>{{tip}}</p>
        </form>
        {% elif status == 1 %}
        已登陆：{{logged.username}}<br/>
        {{logged.charge}}
        {{logged.tem}}
        {{logged.hum}}
        {{logged.pm25}}
        {{logged.pm10}}
        {{logged.device}}
        {{logged.master}}
            <a href='/emsys/logout/'>退出</a>
        {% endif %}
        </body>
</html>
